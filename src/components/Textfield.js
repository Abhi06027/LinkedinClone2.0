import React from "react";

export default function textfield(props) {
  return (
    <div>
     
      <div className="    ">{props.Name}</div>
       <div className="    ">{props.Email}</div>
       <div className="   ">{props.Password}</div>
     
  
    </div>

  );
}
